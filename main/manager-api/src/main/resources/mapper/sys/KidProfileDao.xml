<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xiaozhi.modules.sys.dao.KidProfileDao">

    <!-- Result map for KidProfileEntity -->
    <resultMap id="kidProfileMap" type="xiaozhi.modules.sys.entity.KidProfileEntity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="dateOfBirth" column="date_of_birth"/>
        <result property="gender" column="gender"/>
        <result property="interests" column="interests"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="creator" column="creator"/>
        <result property="createDate" column="create_date"/>
        <result property="updater" column="updater"/>
        <result property="updateDate" column="update_date"/>
    </resultMap>

    <!-- Get all kids by user ID -->
    <select id="getByUserId" resultMap="kidProfileMap">
        SELECT * FROM kid_profile WHERE user_id = #{userId} ORDER BY create_date DESC
    </select>

    <!-- Get kid by ID and user ID (for security check) -->
    <select id="getByIdAndUserId" resultMap="kidProfileMap">
        SELECT * FROM kid_profile WHERE id = #{id} AND user_id = #{userId}
    </select>

</mapper>
