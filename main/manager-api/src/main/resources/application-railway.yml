knife4j:
  production: false
  enable: true
  basic:
    enable: false
    username: renren
    password: 2ZABCDEUgF
  setting:
    enableFooter: false

spring:
  datasource:
    druid:
      #Railway PostgreSQL Configuration
      driver-class-name: org.postgresql.Driver
      # Railway database connection
      url: jdbc:postgresql://nozomi.proxy.rlwy.net:25037/railway?currentSchema=public&sslmode=require&connectTimeout=120&socketTimeout=120&tcpKeepAlive=true&loginTimeout=120
      username: root
      password: OcaVNLKcwNdyElfaeUPqvvfYZiiEHgdm
      initial-size: 5
      max-active: 50
      min-idle: 5
      max-wait: 120000
      pool-prepared-statements: false
      max-pool-prepared-statement-per-connection-size: -1
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      stat-view-servlet:
        enabled: false
      filter:
        stat:
          log-slow-sql: true
          slow-sql-millis: 1000
          merge-sql: false
        wall:
          config:
            multi-statement-allow: true
  data:
    redis:
      # Railway Redis Configuration
      host: maglev.proxy.rlwy.net
      port: 16780
      password: beVfOKcZjMUCBiZIQnvSMivhpSeijpKP
      username: default
      database: 0
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
        shutdown-timeout: 100ms

# Liquibase configuration for Railway
liquibase:
  change-log: classpath:db/changelog/db.changelog-master.yaml
  enabled: true
  drop-first: false
