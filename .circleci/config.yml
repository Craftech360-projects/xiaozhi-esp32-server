version: 2.1

# ==========================================
# 🔄 DYNAMIC PIPELINE SELECTOR
# ==========================================
# This config determines which pipeline(s) to run based on the branch
# ALL BRANCHES: Testing pipeline always runs
# DEV BRANCH: Both testing and production pipelines run
# ==========================================

setup: true

orbs:
  continuation: circleci/continuation@0.3.1

jobs:
  setup_pipelines:
    executor: continuation/default
    steps:
      - checkout
      - run:
          name: "🔄 Determine Pipeline Configuration"
          command: |
            echo "=================================================="
            echo "🔄 XIAOZHI DYNAMIC PIPELINE SELECTOR"
            echo "=================================================="
            echo "📊 BRANCH ANALYSIS:"
            echo "  • Current Branch: $CIRCLE_BRANCH"
            echo "  • Commit: $CIRCLE_SHA1"
            echo "  • Build Number: $CIRCLE_BUILD_NUM"
            echo ""

            if [ "$CIRCLE_BRANCH" = "dev" ]; then
              echo "🎯 DEV BRANCH DETECTED:"
              echo "  ✅ Testing Pipeline (Quality + Security + Performance)"
              echo "  ✅ Production Pipeline (Build + Deploy to Azure)"
              echo ""
              echo "📋 CONFIGURATION: Combined pipelines for dev branch"
              cp .circleci/combined-pipelines.yml .circleci/generated_config.yml
            else
              echo "🎯 FEATURE/OTHER BRANCH DETECTED:"
              echo "  ✅ Testing Pipeline (Quality + Security + Performance)"
              echo "  ⏭️  Production Pipeline (Skipped - not dev branch)"
              echo ""
              echo "📋 CONFIGURATION: Testing pipeline for all branches"
              cp .circleci/testing-config.yml .circleci/generated_config.yml
            fi

            echo "=================================================="
            echo "🚀 Generated Configuration: .circleci/generated_config.yml"

            if [ ! -f ".circleci/generated_config.yml" ]; then
              echo "❌ ERROR: Generated configuration file not found"
              echo "Available files:"
              ls -la .circleci/
              exit 1
            fi

            echo "✅ Configuration validated"
            echo "=================================================="
      - continuation/continue:
          configuration_path: .circleci/generated_config.yml

workflows:
  setup:
    jobs:
      - setup_pipelines