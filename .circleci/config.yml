version: 2.1

# ==========================================
# 🔀 DYNAMIC PIPELINE ROUTER
# ==========================================
# This config determines which pipeline to run based on the branch
# - dev/circleci-validation branches → dev-config.yml
# - main branch → prod-config.yml
# ==========================================

# Setup workflows require the continuation orb
orbs:
  continuation: circleci/continuation@1.0.0

# =========================
# Jobs
# =========================
jobs:
  # Determine which configuration to use based on branch
  setup:
    executor: continuation/default
    steps:
      - checkout
      - run:
          name: Determine Pipeline Configuration
          command: |
            # Get current branch
            BRANCH="${CIRCLE_BRANCH}"
            echo "Current branch: ${BRANCH}"

            # Determine which config to use and create generated_config.yml
            if [[ "${BRANCH}" == "main" ]]; then
              echo "🚀 Using PRODUCTION pipeline configuration"
              cp .circleci/prod-config.yml .circleci/generated_config.yml
            elif [[ "${BRANCH}" == "dev" || "${BRANCH}" == "circleci-validation" ]]; then
              echo "🔧 Using DEVELOPMENT pipeline configuration"
              cp .circleci/dev-config.yml .circleci/generated_config.yml
            else
              echo "⚠️  Branch '${BRANCH}' not configured for CI/CD"
              echo "Only main, dev, and circleci-validation branches trigger pipelines"
              exit 0
            fi

            # Verify config file exists
            if [ ! -f ".circleci/generated_config.yml" ]; then
              echo "❌ ERROR: Failed to create generated config"
              exit 1
            fi

            echo "✅ Generated configuration ready"

      - continuation/continue:
          configuration_path: .circleci/generated_config.yml

# =========================
# Workflows
# =========================
workflows:
  # Setup workflow runs first to determine which pipeline to execute
  setup-workflow:
    jobs:
      - setup:
          filters:
            branches:
              only:
                - main
                - dev
                - circleci-validation
